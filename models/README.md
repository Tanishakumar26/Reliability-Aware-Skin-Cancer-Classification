# Model Weights

Pretrained model weights are not included due to GitHub file size limits.

To reproduce results:

1. Train the model:
python src/train_resnet.py

2. The trained weights will be saved as:
models/model.pth
